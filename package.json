{
  "name": "pmo-ai-ticket",
  "version": "1.0.0",
  "description": "AI 小票根",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./public/tailwind-built.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}